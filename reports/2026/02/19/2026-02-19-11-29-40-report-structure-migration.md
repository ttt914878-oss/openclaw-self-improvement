# Report Structure Migration

- reportsの保管先を `reports/YYYY/MM/DD/` に変更。
- 命名規則を `YYYY-MM-DD-HH-mm-ss-*.md` に統一。
- 既存の直下レポートを日付階層へ移動済み。
- memoryログ形式を `・HH-mm：...` に統一。
- 報告ルールを「1時間ごとの定期サマリー + 重要イベント即時」に更新。
